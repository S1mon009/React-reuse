export const name = "usePromiseAll";
export const link = "/docs/hooks/usepromiseall";
export const createdAt = "2024-10-17";
export const description =
  "UsePromiseAll to hook, który umożliwia równoległe wykonywanie wielu obietnic (promises), oferując prosty sposób śledzenia stanu ładowania, sukcesu i błędów.";

import { Separator } from "@/components/ui/separator";
import Heading from "@/components/docs/heading/heading";
import CodeTabs from "@/components/code/code-tabs";
import { roughNotationColor } from "@/config/rought-notation-color";

<Heading title={name} color={roughNotationColor} />

## Opis

`UsePromiseAll` to hook, który umożliwia równoległe wykonywanie wielu obietnic (promises), oferując prosty sposób śledzenia stanu ładowania, sukcesu i błędów. Po rozwiązaniu wszystkich obietnic (lub ich odrzuceniu), hook zwraca wyniki lub błąd oraz stan ładowania.

<Separator className="my-5" aria-hidden="true" />

<CodeTabs
  defaultValue="hook"
  triggers={[
    {
      value: "hook",
      title: "Hook",
    },
    {
      value: "usage",
      title: "Zastosowanie",
    },
  ]}
  contents={[
    {
      value: "hook",
      code: `/data/hooks/${name.toLowerCase()}/hook.ts`,
      ariaLabel: "Hook code scroll area",
    },
    {
      value: "usage",
      code: `/data/hooks/${name.toLowerCase()}/usage.txt`,
      ariaLabel: "Usage example scroll area",
    },
  ]}
/>

## API

- `results: T[] | null` - Rozwiązane wartości wszystkich obietnic lub `null`, jeśli trwa ładowanie.
- `error: Error | null` - Każdy napotkany błąd w trakcie wykonywania obietnic, lub `null` w przypadku braku błędów.
- `isLoading: boolean` - Wartość logiczna reprezentująca stan ładowania (`true` podczas oczekiwania na obietnice, `false`, gdy wszystkie zostaną rozwiązane lub jedna zostanie odrzucona).

## Parametry

- `promises: Promise<T>[]` - Tablica obietnic do wykonania równocześnie.
