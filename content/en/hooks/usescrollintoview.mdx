export const name = "usescrollIntoView";
export const link = "/docs/hooks/usescrollintoview";
export const createdAt = "2024-10-17";
export const description =
  "The useScrollIntoView hook is designed to scroll an element (referenced by a ref) into view with customizable behavior.";

import { Separator } from "@/components/ui/separator";
import Heading from "@/components/docs/heading/heading";
import CodeTabs from "@/components/code/code-tabs";
import { roughNotationColor } from "@/config/rought-notation-color";

<Heading title={name} color={roughNotationColor} />

## Description

The `useScrollIntoView` hook is designed to scroll an element (referenced by a ref) into view with customizable behavior. It provides a callback function that triggers the scroll action, and it allows you to specify the scroll behavior (auto or smooth), and the element's alignment once it is scrolled into view (positioning options like start, center, end, or nearest for both block and inline directions).

<Separator className="my-5" aria-hidden="true" />

<CodeTabs
  defaultValue="hook"
  triggers={[
    {
      value: "hook",
      title: "Hook",
    },
    {
      value: "usage",
      title: "Usage",
    },
  ]}
  contents={[
    {
      value: "hook",
      code: `/data/hooks/${name.toLowerCase()}/hook.ts`,
      ariaLabel: "Hook code scroll area",
    },
    {
      value: "usage",
      code: `/data/hooks/${name.toLowerCase()}/usage.txt`,
      ariaLabel: "Usage example scroll area",
    },
  ]}
/>

## API

- `scrollToElement(): void` - A callback function that, when invoked, scrolls the referenced element into view.

## Parameters

- `ref: React.RefObject<HTMLElement>` - A reference to the element that should be scrolled into view.
- `behavior?: auto | smooth` - The scrolling behavior. Defaults to smooth.
- `block?: start | center | end | nearest` - The vertical alignment of the element when scrolled into view. Defaults to start.
- `inline: start | center | end | nearest` - The horizontal alignment of the element when scrolled into view. Defaults to nearest.
