export const name = "usescrollIntoView";
export const link = "/docs/hooks/usescrollintoview";
export const createdAt = "2024-10-17";
export const description =
  "Hook useScrollIntoView służy do przewijania elementu (reprezentowanego przez ref) do widoku z możliwością dostosowania zachowania.";

import { Separator } from "@/components/ui/separator";
import Heading from "@/components/docs/heading/heading";
import CodeTabs from "@/components/code/code-tabs";
import { roughNotationColor } from "@/config/rought-notation-color";

<Heading title={name} color={roughNotationColor} />

## Opis

Hook `useScrollIntoView` służy do przewijania elementu (reprezentowanego przez ref) do widoku z możliwością dostosowania zachowania. Zapewnia funkcję zwrotną, która uruchamia akcję przewijania, a także pozwala określić sposób przewijania (auto lub smooth) oraz wyrównanie elementu po przewinięciu do widoku (opcje pozycjonowania, takie jak start, center, end lub nearest dla obu kierunków: blokowego i inline).

<Separator className="my-5" aria-hidden="true" />

<CodeTabs
  defaultValue="hook"
  triggers={[
    {
      value: "hook",
      title: "Hook",
    },
    {
      value: "usage",
      title: "Zastosowanie",
    },
  ]}
  contents={[
    {
      value: "hook",
      code: `/data/hooks/${name.toLowerCase()}/hook.ts`,
      ariaLabel: "Hook code scroll area",
    },
    {
      value: "usage",
      code: `/data/hooks/${name.toLowerCase()}/usage.txt`,
      ariaLabel: "Usage example scroll area",
    },
  ]}
/>

## API

- `scrollToElement(): void` - Funkcja zwrotna, która po wywołaniu przewija wskazany element do widoku.

## Parametry

- `ref: React.RefObject<HTMLElement>` - Referencja do elementu, który powinien zostać przewinięty do widoku.
- `behavior?: auto | smooth` - Zachowanie przewijania. Domyślnie smooth.
- `block?: start | center | end | nearest` - Wyrównanie pionowe elementu po przewinięciu do widoku. Domyślnie start.
- `inline: start | center | end | nearest` - Wyrównanie poziome elementu po przewinięciu do widoku. Domyślnie nearest.
