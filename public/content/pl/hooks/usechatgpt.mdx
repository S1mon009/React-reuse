export const name = "useChatGPT";
export const link = "/docs/hooks/usechatgpt";
export const createdAt = "2024-10-17";
export const description =
  "UseChatGPT to niestandardowy hook React, który pozwala na interakcję z API ChatGPT.";

import { Separator } from "@/components/ui/separator";
import Heading from "@/components/docs/heading/heading";
import CodeTabs from "@/components/code/code-tabs";
import { roughNotationColor } from "@/config/rought-notation-color";

<Heading title={name} color={roughNotationColor} />

## Opis

`UseChatGPT` to niestandardowy hook React, który pozwala na interakcję z API ChatGPT. Zarządza procesem przesyłania danych użytkownika do API, obsługuje stany ładowania i błędu oraz przechowuje odpowiedź zwróconą przez API. Może być łatwo zintegrowany z dowolnym komponentem React, aby umożliwić funkcjonalność chatbota.

<Separator className="my-5" aria-hidden="true" />

<CodeTabs
  defaultValue="hook"
  triggers={[
    {
      value: "hook",
      title: "Hook",
    },
    {
      value: "usage",
      title: "Zastosowanie",
    },
  ]}
  contents={[
    {
      value: "hook",
      code: `/data/hooks/${name.toLowerCase()}/hook.ts`,
      ariaLabel: "Hook code scroll area",
    },
    {
      value: "usage",
      code: `/data/hooks/${name.toLowerCase()}/usage.txt`,
      ariaLabel: "Usage example scroll area",
    },
  ]}
/>

## API

- `apiKey: string` - Klucz API używany do uwierzytelniania żądań do OpenAI API.

## Parametry

- `sendMessage(message: string): Promise<void>` - Funkcja, która przyjmuje ciąg znaków jako wiadomość, wysyła ją do API ChatGPT i aktualizuje stan odpowiedzi.
- `response: string | null` - Odpowiedź zwrócona przez API ChatGPT lub null, jeśli brak odpowiedzi.
- `isLoading: boolean`- Flaga logiczna wskazująca, czy żądanie do API ChatGPT jest w trakcie przetwarzania.
- `error: string | null` - Komunikat o błędzie, jeśli wystąpił błąd podczas żądania do API, lub null, jeśli brak błędu.
