export const name = "useDebounce";
export const link = "/docs/hooks/usedebounce";
export const createdAt = "2024-10-17";
export const description =
  "Hook useDebounce opóźnia aktualizację wartości do momentu upłynięcia określonego czasu od ostatniej zmiany wartości.";

import { Separator } from "@/components/ui/separator";
import Heading from "@/components/docs/heading/heading";
import CodeTabs from "@/components/code/code-tabs";
import { roughNotationColor } from "@/config/rought-notation-color";

<Heading title={name} color={roughNotationColor} />

## Opis

Hook `useDebounce` opóźnia aktualizację wartości do momentu upłynięcia określonego czasu od ostatniej zmiany wartości. Jest to przydatne w scenariuszach takich jak filtrowanie danych wprowadzanych przez użytkownika lub zapobieganie wielokrotnym wywołaniom API podczas szybkich zmian.

<Separator className="my-5" aria-hidden="true" />

<CodeTabs
  defaultValue="hook"
  triggers={[
    {
      value: "hook",
      title: "Hook",
    },
    {
      value: "usage",
      title: "Zastosowanie",
    },
  ]}
  contents={[
    {
      value: "hook",
      code: `/data/hooks/${name.toLowerCase()}/hook.ts`,
      ariaLabel: "Hook code scroll area",
    },
    {
      value: "usage",
      code: `/data/hooks/${name.toLowerCase()}/usage.txt`,
      ariaLabel: "Usage example scroll area",
    },
  ]}
/>

## API

- `debouncedValue: T` - wartość, która aktualizuje się dopiero po określonym opóźnieniu.

## Parametry

- `value: T` - Bieżąca wartość do opóźnienia. Może być dowolnego typu (`T`).
- `delay: number` - Opóźnienie w milisekundach.
