export const name = "<Delay/>";
export const link = "/docs/utilities/delay";
export const createdAt = "2024-10-17";
export const description =
  "The Delay component is a utility that renders its children after a specified delay.";

import { Separator } from "@/components/ui/separator";
import Heading from "@/components/docs/heading/heading";
import CodeTabs from "@/components/code/code-tabs";
import { roughNotationColor } from "@/config/rought-notation-color";

<Heading title={name} color={roughNotationColor} />

## Description

The `Delay` component is a utility that renders its children after a specified delay. It is useful when you want to defer the rendering of certain components for UX or performance reasons. This component accepts the `ms prop, which determines the delay in milliseconds before the children are rendered. During the delay period, the component can show a loading indicator or nothing.

<Separator className="my-5" aria-hidden="true" />

<CodeTabs
  defaultValue="util"
  triggers={[
    {
      value: "util",
      title: "Util",
    },
    {
      value: "usage",
      title: "Usage",
    },
  ]}
  contents={[
    {
      value: "util",
      code: `/data/utilities/${name.slice(1, -2).toLowerCase()}/util.tsx`,
      ariaLabel: "Util code scroll area",
    },
    {
      value: "usage",
      code: `/data/utilities/${name.slice(1, -2).toLowerCase()}/usage.txt`,
      ariaLabel: "Usage example scroll area",
    },
  ]}
/>

## Parameters

- `ms: number` - The delay in milliseconds before rendering the children.
