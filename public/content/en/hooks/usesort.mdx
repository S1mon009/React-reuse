export const name = "useSort";
export const link = "/docs/hooks/usesort";
export const createdAt = "2024-10-17";
export const description =
  "The useSort hook helps you sort lists of data easily by providing a sorted version of the list based on a specified sorting function.";

import { Separator } from "@/components/ui/separator";
import Heading from "@/components/docs/heading/heading";
import CodeTabs from "@/components/code/code-tabs";
import { roughNotationColor } from "@/config/rought-notation-color";

<Heading title={name} color={roughNotationColor} />

## Description

The `useSort` hook helps you sort lists of data easily by providing a sorted version of the list based on a specified sorting function. It also provides controls for toggling between ascending and descending order.

<Separator className="my-5" aria-hidden="true" />

<CodeTabs
  defaultValue="hook"
  triggers={[
    {
      value: "hook",
      title: "Hook",
    },
    {
      value: "usage",
      title: "Usage",
    },
  ]}
  contents={[
    {
      value: "hook",
      code: `/data/hooks/${name.toLowerCase()}/hook.ts`,
      ariaLabel: "Hook code scroll area",
    },
    {
      value: "usage",
      code: `/data/hooks/${name.toLowerCase()}/usage.txt`,
      ariaLabel: "Usage example scroll area",
    },
  ]}
/>

## API

- `sortedItems: T[]` - The sorted version of the provided list.
- `order: asc | desc` - The current sorting order.
- `toggleOrder(): void` - A function to toggle between ascending (asc) and descending (desc) order.

## Parameters

- `items: T[]` - The list of items to be sorted.
- `sortFn: (a: T, b: T): number` - A sorting function that defines how to compare two items in the list. Should return a negative number if a comes before b, a positive number if a comes after b, and 0 if they are equal.
- `initialOrder?: asc | desc` - The initial sorting order. Defaults to 'asc' (ascending).
