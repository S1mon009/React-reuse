export const name = "useClickOutside";
export const link = "/docs/hooks/useclickoutside";
export const createdAt = "2024-10-17";
export const description =
  "A custom hook that triggers a callback function when a click is detected outside of the element referenced by the ref.";

import { Separator } from "@/components/ui/separator";
import Heading from "@/components/docs/heading/heading";
import CodeTabs from "@/components/code/code-tabs";
import { roughNotationColor } from "@/config/rought-notation-color";

<Heading title={name} color={roughNotationColor} />

## Description

The `useClickOutside` is a custom hook that triggers a callback function when a click is detected outside of the element referenced by the ref.

<Separator className="my-5" aria-hidden="true" />

<CodeTabs
  defaultValue="hook"
  triggers={[
    {
      value: "hook",
      title: "Hook",
    },
    {
      value: "usage",
      title: "Usage",
    },
  ]}
  contents={[
    {
      value: "hook",
      code: `/data/hooks/${name.toLowerCase()}/hook.ts`,
      ariaLabel: "Hook code scroll area",
    },
    {
      value: "usage",
      code: `/data/hooks/${name.toLowerCase()}/usage.txt`,
      ariaLabel: "Usage example scroll area",
    },
  ]}
/>

## Parameters

- `ref: RefObject<HTMLElement>` - A React ref object pointing to the HTML element to watch for outside clicks.
- `callback(): void` - A function to be called when a click outside the referenced element is detected.
