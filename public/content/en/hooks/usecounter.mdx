export const name = "useCounter";
export const link = "/docs/hooks/usecounter";
export const createdAt = "2024-10-17";
export const description =
  "The useCounter hook provides an easy way to manage counter logic, such as incrementing, decrementing, resetting, and setting a specific value.";

import { Separator } from "@/components/ui/separator";
import Heading from "@/components/docs/heading/heading";
import CodeTabs from "@/components/code/code-tabs";
import { roughNotationColor } from "@/config/rought-notation-color";

<Heading title={name} color={roughNotationColor} />

## Description

The `useCounter` hook provides an easy way to manage counter logic, such as incrementing, decrementing, resetting, and setting a specific value.

<Separator className="my-5" aria-hidden="true" />

<CodeTabs
  defaultValue="hook"
  triggers={[
    {
      value: "hook",
      title: "Hook",
    },
    {
      value: "usage",
      title: "Usage",
    },
  ]}
  contents={[
    {
      value: "hook",
      code: `/data/hooks/${name.toLowerCase()}/hook.ts`,
      ariaLabel: "Hook code scroll area",
    },
    {
      value: "usage",
      code: `/data/hooks/${name.toLowerCase()}/usage.txt`,
      ariaLabel: "Usage example scroll area",
    },
  ]}
/>

## API

- `count: number` - The current count value.
- `increment(): void` - Increases the count by 1.
- `decrement(): void` - Decreases the count by 1.
- `reset(): void` - Resets the count to the initialValue.
- `set(value: number): void` - Sets the count to a specified value.

## Parameters

- `initialValue?: number` - The initial value of the counter. Defaults to 0.
